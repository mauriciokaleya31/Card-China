services:
  app:
    container_name: card-china-portal
    build: .
    pull_policy: build
    ports:
      - "${HOST_PORT:-8080}:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/id_cards.db
    restart: always
